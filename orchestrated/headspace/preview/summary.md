# Headspace Mobile

This workspace is the source of truth for mobile design synthesis.

- Platforms: flutter, swiftui, compose
- Inspiration sources: 2
- Inspiration assets: 24

## Next steps

1. Review `inspirations/index.json`.
2. Add idea cards with `scripts/add_idea.py`.
3. Refine the files under `contract/` and `screens/`.
4. Tighten the mappings under `platforms/`.
5. Run `scripts/validate_design_contract.py --output-dir orchestrated/headspace` before implementation work.
